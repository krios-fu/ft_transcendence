export enum FriendshipStatus {
    PENDING = "pending",
    CONFIRMED = "confirmed",
    REFUSED = "refused",
    BLOCKED = "blocked"
};

export enum Category {
	Pending,
	Iron,
	Bronze,
	Silver,
	Gold,
	Platinum
}