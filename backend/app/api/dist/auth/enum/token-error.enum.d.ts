export declare enum TokenError {
    NO_TOKEN_OR_USER = "No token available for user",
    TOKEN_EXPIRED = "Token has expired",
    TOKEN_INVALID = "Token not assigned to current user",
    TOKEN_BLACKLISTED = "Token currrently blacklisted from server"
}
