import { PrivateroomroleGuard } from './privateroomrole.guard';

describe('PrivateroomroleGuard', () => {
  it('should be defined', () => {
    expect(new PrivateroomroleGuard()).toBeDefined();
  });
});
