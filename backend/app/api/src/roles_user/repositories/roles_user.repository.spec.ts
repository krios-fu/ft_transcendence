import { RolesUserRepository } from './roles_user.repository';

describe('RolesUserRepository', () => {
  it('should be defined', () => {
    expect(new RolesUserRepository()).toBeDefined();
  });
});
