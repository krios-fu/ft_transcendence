import { PrivateRoomStrategy } from './private-room.strategy';

describe('PrivateRoomStrategy', () => {
  it('should be defined', () => {
    expect(new PrivateRoomStrategy()).toBeDefined();
  });
});
