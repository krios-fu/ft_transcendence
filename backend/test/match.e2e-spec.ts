import {Get} from "@nestjs/common";

describe('/match (e2e)', () => {
    it ('[ placeholder ]', () => {
        expect(1).toBe(1);
    });

    describe('[ GET /match ]', () => {

    });

    describe('[ GET /match/:id ]', () => {

    });
});