import {Get} from "@nestjs/common";

describe('/chat (e2e)', () => {
    it ('[ placeholder ]', () => {
        expect(1).toBe(1);
    });

    describe('[ GET /chat ]', () => {

    });

    describe('[ GET /chat/:id ]', () => {

    });
});